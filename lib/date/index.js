/**
 * @module Index
 * @description Index of Date library
 * @author KenySushant
 */
const convert = require('./convert');
const normalize = require('./normalize');

module.exports = {
  convert,
  normalize,
};
